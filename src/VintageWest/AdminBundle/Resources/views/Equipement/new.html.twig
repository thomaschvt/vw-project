{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Equipement creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('equipement') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
